NCAAM MODEL - MASTER HANDOFF (PREGAME FOCUS)
Last updated: 2026-03-28
Owner intent: Preserve current progress, avoid regression, resume quickly in future sessions and next season.

Update note (2026-03-28):
- The retired 2H script set has now been physically removed from the repository.
- Post-deletion verification completed with two clean pregame-only audit passes:
  - Active script compile checks: PASS
  - Workbook/lines/baselines/cache integrity audit: PASS
  - Cached predictor smoke tests: PASS


!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
!! CRITICAL — READ THIS BEFORE ANYTHING ELSE                         !!
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!

THE 2H LIVE-BET HALFTIME MODEL IS DEAD. IT IS NOT BEING USED.
IT IS NOT BEING MAINTAINED. IT IS NOT PART OF THIS PROJECT.

Do NOT:
- Reference, run, fix, audit, or improve any halftime prediction pipeline.
- Touch WinnerCorrect, Pred2HRange, Actual2H, TwoH_Error, PredWinner,
  PredMargin, Pred2HRange_Narrow, PredTotalRange, Total_Error, Confidence,
  TriggerDecision, TriggerName, TriggerHistHit, TriggerHistN, WageredFlag,
  TriggerVersion, StakeTier columns in the workbook — they are dead weight.
- Run analyze_results_data.py — it audits 2H model columns only, not the
  current pregame model. Its output means nothing here.
- Run log_prediction_to_results_v1.py — this writes 2H halftime predictions.
- Run trigger_gate_from_workbook_v1.py — 2H trigger/stake gate. Defunct.
- Run step4_pull_halftime_pbp_v2.py — live halftime PBP pull. Defunct.
- Run backfill_halftime_predictions_for_date_v1.py — 2H backfill. Defunct.
- Run postgame_export_to_log_v1.py — 2H postgame export. Defunct.
- Run rebuild_reports_from_workbook_v2.py — 2H report rebuild. Defunct.
- Run rebuild_results_from_reports_v1.py — 2H results rebuild. Defunct.
- Run overwrite_predictions_in_place_v1.py — 2H overwrite tool. Defunct.
- Run update_all_predictions_ml.py — bulk-applies 2H model to workbook. Defunct.
- Run analyze_trigger_cohorts_v1.py — 2H trigger analysis. Defunct.
- Run explore_tight_range_triggers_v1.py — 2H range trigger exploration. Defunct.

These scripts and columns exist only as historical artifacts. Touching them
will actively corrupt thinking about what this model does and how to improve it.
The 2H model is entirely separate from the pregame model. The workbook columns
it wrote are irrelevant to pregame model training. Do not attempt to reconcile
them, audit them, or derive meaning from their numbers.

The ONLY pregame model input from the workbook is:
  Date | Home | Away | GameID | ActualTotal

That is it. Everything else the pregame model needs comes from:
  data/processed/market_lines/canonical_lines.csv
  data/processed/baselines/lastN_<window>_YYYY-MM-DD.json
  data/raw/pbp_live/<GameID>/ (historical baseline PBP feature files)

!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!


======================================================================
1) EXECUTIVE SUMMARY (READ THIS FIRST)
======================================================================

Current strategic focus is PRE-GAME totals prediction and directional O/U decision support.

The 2H live-bet halftime model has been permanently retired. Do not return to it.

The current production-ready workflow for pregame totals is cache-backed:
- First run after data changes trains (slow, about 7-10 minutes).
- Repeat runs reuse cached model (fast, seconds).

Primary objective:
- Consistent, reproducible pregame predictions vs known line inputs.
- Preserve statistical discipline and avoid accidental lookahead.
- Build toward productization (paywall app/site) without changing core logic impulsively.


======================================================================
2) WHAT HAS BEEN PERMANENTLY RETIRED (DO NOT RESURRECT)
======================================================================

The entire 2H live-bet halftime pipeline. This is NOT a de-prioritized backburner
item — it is a CLOSED direction. Future sessions must not open it back up.

Retired because:
A) It was a fundamentally different product (live in-game betting, not pregame).
B) Its workbook columns pollute analysis of the pregame model.
C) Auditing/fixing its outputs wasted significant session time (e.g. the workbook
   collision issue, 1,425 corrupted rows, were caused by 2H model scripts writing
   to the wrong columns — a direct cost of keeping the 2H pipeline alive).
D) There is no path to using it that does not risk contaminating the pregame model
   training data, feature logic, or evaluation metrics.

Also retired:
- Chasing exact-point tight ranges as the main success metric.
- Re-running heavy pregame full retrains for every 1-2 game query (replaced by cache).


======================================================================
3) WHAT IS THE ACTIVE DIRECTION RIGHT NOW
======================================================================

A) Pregame totals model prediction workflow
- Input: team matchup + market total line.
- Output: model total, signed gap vs line, directional lean (OVER/UNDER), and trigger tier.

B) Cache-first execution
- Uses a fingerprint to decide train vs reuse.
- Reuses persisted model when source inputs are unchanged.

C) Workflow simplification
- One-click VS Code task added for cached pregame predictions.
- Reduced operator confusion during short pre-tip windows.


======================================================================
4) KEY FILES TO KNOW (HIGH PRIORITY)
======================================================================

Core active files:
- Scripts/predict_pregame_totals_cached_v1.py
- Scripts/vscode_task_runner.ps1
- .vscode/tasks.json
- Scripts/evaluate_pregame_total_model_v1.py
- Scripts/evaluate_window_optimization_v1.py
- Scripts/analyze_pregame_model_depth_v1.py
- Scripts/model_feature_utils.py

Cache artifacts:
- models/pregame_total_cache/pregame_total_rf_w5.pkl
- models/pregame_total_cache/pregame_total_rf_w5.meta.json

Primary data dependencies:
- logs/NCAAM Results.xlsx
- data/processed/market_lines/canonical_lines.csv
- data/processed/baselines/lastN_5_YYYY-MM-DD.json (and related lastN files)
- data/raw/pbp_live/... (priors/features support)

Data lineage and storage contracts (authoritative):
- Source: workbook game log and results in logs/NCAAM Results.xlsx (sheet: Game_Log).
- Source: historical pregame lines scraped from Covers via Scripts/ncaab_historical_lines_covers_v1.py.
- Optional source: staged sportsbook JSON flow (SGO) via Scripts/stage_market_lines_sgo_v1.py -> Scripts/merge_staged_lines_to_canonical_v1.py.
- Canonical store: data/processed/market_lines/canonical_lines.csv.
- Baseline store: data/processed/baselines/lastN_<window>_YYYY-MM-DD.json.

Canonical market line schema (from code and current file header):
- game_id
- date
- away_seo
- home_seo
- spread_home
- spread_away
- ml_home
- ml_away
- total_game
- total_2h
- sgo_event_id
- source_file
- staged_timestamp

Schema and integration docs:
- MARKET_LINES_SCHEMA.md
- MARKET_LINES_INTEGRATION.md

Market-line ingestion scripts (important):
- Scripts/ncaab_historical_lines_covers_v1.py
- Scripts/merge_staged_lines_to_canonical_v1.py

Runbook/doc files:
- README.md (contains current runbook section and production vs experimental split)
- This file: MASTER_HANDOFF_PREGAME_MODEL_2026-03-27.txt


======================================================================
5) WHAT WAS JUST IMPLEMENTED (IMPORTANT)
======================================================================

1) New cache-backed pregame predictor created:
- Scripts/predict_pregame_totals_cached_v1.py

2) Cache behavior validated:
- First run (cache miss): training completed around 10.04 minutes.
- Repeat run (cache hit): approximately 1.88 seconds.

3) VS Code runner wired:
- New action in Scripts/vscode_task_runner.ps1: pregame_cached
- New task in .vscode/tasks.json:
  NCAAM: Pregame totals (cached model) [READ-ONLY]
- Prompt-driven game spec input added for one or two games.

4) Operational documentation improved:
- README top section now differentiates what to use daily vs research scripts.


======================================================================
6) EXACT OPERATOR COMMANDS
======================================================================

A) Preferred one-click method (VS Code Tasks UI)
Task label:
NCAAM: Pregame totals (cached model) [READ-ONLY]

Prompts:
- pregameWindow (default 5)
- pregameGameSpec1 (required)
- pregameGameSpec2 (optional)

Game spec format (critical):
home,away,line,label,tipoff

Example:
michigan-st,uconn,136.5,Michigan State vs UConn,7:45 PM CST


B) Direct terminal method (same logic)
powershell -NoProfile -ExecutionPolicy Bypass -File Scripts/vscode_task_runner.ps1 -Action pregame_cached -Window 5 -GameSpec "michigan-st,uconn,136.5,Michigan State vs UConn,7:45 PM CST" -GameSpec2 "tennessee,iowa-st,139.5,Tennessee vs Iowa State,9:25 PM CST"


C) Script-only method (bypass PS runner)
"c:/NCAA Model/.venv/Scripts/python.exe" Scripts/predict_pregame_totals_cached_v1.py --window 5 --game "michigan-st,uconn,136.5,Michigan State vs UConn,7:45 PM CST" --game "tennessee,iowa-st,139.5,Tennessee vs Iowa State,9:25 PM CST"


D) Force retrain when intentionally needed
Add: --force-retrain

Use this only when source data has changed and you want a fresh model immediately.


E) Covers.com scraper runbook (pregame line source)

Current pregame historical line source is Covers-derived data written to canonical lines.

Primary scraper:
"c:/NCAA Model/.venv/Scripts/python.exe" Scripts/ncaab_historical_lines_covers_v1.py --since YYYY-MM-DD

Single-date scrape:
"c:/NCAA Model/.venv/Scripts/python.exe" Scripts/ncaab_historical_lines_covers_v1.py --date YYYY-MM-DD

Full workbook date refresh (expensive):
"c:/NCAA Model/.venv/Scripts/python.exe" Scripts/ncaab_historical_lines_covers_v1.py --all

Dry-run validation (no file writes):
"c:/NCAA Model/.venv/Scripts/python.exe" Scripts/ncaab_historical_lines_covers_v1.py --date YYYY-MM-DD --dry-run

Output target used by evaluators/pregame workflows:
- data/processed/market_lines/canonical_lines.csv

Important semantics:
- This path is pregame closing-style totals from Covers source.
- It is not halftime in-play market capture.
- 2H totals are typically blank in this Covers path.

When to run scraper:
1) After adding new historical dates/games to workbook.
2) Before pregame evaluation runs that need updated line coverage.
3) Before intentional model retrain if line universe changed materially.

Sanity check after scrape:
1) Confirm canonical file updated timestamp.
2) Confirm expected date range present.
3) Re-run pregame evaluator and check joined-row counts.

Coverage audit command (recommended before large backfills):
"c:/NCAA Model/.venv/Scripts/python.exe" Scripts/audit_covers_matching_v1.py --recent-dates 21 --out data/logs/covers_slug_audit_recent21.json

Audit interpretation:
- Use rolling-window coverage (for example recent 21 dates) as the main health signal.
- Treat single-date snapshots as potentially noisy due to workbook coverage/date composition.
- Prioritize fixes from top_missing_mapped_slugs and top_unmatched in the audit JSON.


======================================================================
7) HOW CACHE INVALIDATION WORKS (OPERATOR MENTAL MODEL)
======================================================================

The cached predictor computes a fingerprint from major inputs and script version details.
If fingerprint matches existing cache metadata:
- Cache HIT -> fast load and predict.

If fingerprint differs or cache missing:
- Cache MISS -> full train then save cache.

Expected runtime pattern:
- Miss: ~7-10 minutes (can vary with machine and I/O).
- Hit: seconds.


======================================================================
8) CURRENT STATISTICAL POSTURE (DO NOT MISINTERPRET)
======================================================================

Important distinctions:
1) Halftime-informed models and pregame models are different products.
2) Pregame edge is harder; evaluate honestly on pre-tip information only.
3) Prior analyses showed selective thresholding matters more than raw MAE obsession.

Core discipline:
- Measure directional correctness vs line where relevant.
- Prefer policy-driven bet filters (gap thresholds, sample minimums).
- Avoid over-claiming from small cohorts.

Do not promise unrealistic sustained hit rates.
Use conservative language and forward validation principles.


======================================================================
9) KNOWN RISKS / CAVEATS
======================================================================

A) Naming/slug mismatch risk
- Team SEO key mismatch can break matchup lookups.
- Always use workbook-style slugs (example: michigan-st, iowa-st, uconn/connecticut depending dataset conventions).

B) Data freshness risk
- If baselines/lines/workbook are stale, predictions can still run but context may be outdated.

C) Market semantics risk
- Some historical evaluations used pregame closing-like lines from Covers source.
- Do not conflate that with live in-play line evaluations.

D) Scope drift risk
- New sessions can accidentally drift back into live 2H architecture changes.
- Keep focus on pregame unless user explicitly pivots.


======================================================================
10) NEXT-SEASON RESTART PLAN (NO GUESSWORK)
======================================================================

If returning months later (including next season), follow this order:

Step 1: Environment sanity
- Confirm .venv works.
- Confirm workbook and key data files are present.

Step 2: Data refresh for new season
- Run slate prep and baseline flows for target date(s).
- Ensure market lines and baseline files exist for the dates you will predict.

Step 3: Cache bootstrap
- Run cached pregame task once (expect cache miss/training).
- Confirm cache files appear in models/pregame_total_cache.

Step 4: Cache-hit confirmation
- Run same task again with same inputs.
- Confirm cache HIT in output and fast runtime.

Step 5: Statistical re-baseline
- Run evaluator scripts on the newly expanded season data.
- Re-establish policy thresholds with enough sample before production claims.

Step 6: Freeze and operate
- Freeze chosen thresholds/model config for a defined evaluation period.
- Avoid constant parameter churn during operation windows.


======================================================================
11) WHAT TO DO FIRST IN ANY NEW CHAT SESSION
======================================================================

Prompt the new session to do these actions in order:

1) Read this file fully.
2) Confirm current objective remains pregame model only.
3) Run the cached pregame task for a known matchup and confirm cache status.
4) Report:
   - cache status (HIT/MISS)
   - runtime
   - prediction output block
5) If MISS unexpectedly repeats, debug fingerprint inputs and cache metadata.

Suggested quick check command:
powershell -NoProfile -ExecutionPolicy Bypass -File Scripts/vscode_task_runner.ps1 -Action pregame_cached -Window 5 -GameSpec "michigan-st,uconn,136.5,Michigan State vs UConn,7:45 PM CST"


======================================================================
12) ROADMAP: HOW TO HONE FURTHER (PRIORITIZED)
======================================================================

Priority 1: Stability and repeatability
- Keep cache pathway stable and observable.
- Add lightweight structured output logging (prediction, line, gap, timestamp, model version).

Priority 2: Policy hardening
- Continue threshold policy evaluation (FULL/HALF/PASS) on expanding samples.
- Add confidence bands for recommendation strength tied to sample depth.

Priority 3: Better model governance
- Version model snapshots explicitly.
- Record training window/date span and feature set fingerprint.
- Track performance drift week-over-week.

Priority 4: Productization prep (paywall app/site)
- Create API-like wrapper around cached predictor.
- Add request validation (slug, line, date format).
- Return deterministic JSON response schema.
- Persist all requests/predictions for auditability.

Priority 5: Controlled feature expansion
- Add features only with strict forward-validation gains.
- Avoid broad feature churn without policy-level improvement.


======================================================================
13) PRODUCTIZATION DIRECTION (APP/WEBSITE)
======================================================================

Recommended architecture path:

A) Prediction service layer
- Input payload: matchup, line, window, optional metadata.
- Output payload: model_total, baseline_total, gap, direction, trigger_tier, model_version, cache_status.

B) Persistence and audit
- Store every prediction request and response.
- Include line source and timestamp.
- Keep immutable logs for post-hoc performance tracking.

C) Access control/paywall
- Separate auth/subscription from model logic.
- Keep model execution stateless where possible.

D) Monitoring
- Alert on repeated cache misses.
- Alert on runtime spikes and missing data dependencies.

E) Safety in messaging
- No guaranteed win claims.
- Show uncertainty and policy rationale clearly.


======================================================================
14) TROUBLESHOOTING QUICK LIST
======================================================================

Issue: Prediction script retrains every time.
- Check cache metadata file exists.
- Check fingerprint inputs changed (workbook mtime/size, lines file mtime/size, latest baseline info, script version).
- Use --show-cache-info for diagnostics.

Issue: Team not found / bad matchup.
- Verify SEO team keys exactly as used in workbook/baselines.
- Use known slugs from prior successful runs.

Issue: Slow run unexpectedly.
- If cache hit expected, verify no data files touched recently.
- Confirm no force-retrain flag present.

Issue: Inconsistent numbers across sessions.
- Ensure same window and same line values.
- Ensure same underlying data snapshot and no mid-run file changes.


======================================================================
15) NON-NEGOTIABLE OPERATING PRINCIPLES
======================================================================

1) Keep pregame focus unless explicitly changed.
2) Prefer cache-backed runs for operational predictions.
3) Keep validation honest (time-aware, no leakage).
4) Do not silently change thresholds in production messaging.
5) Log everything needed for reproducibility and accountability.


======================================================================
16) QUICK REFERENCE (COPY/PASTE BLOCK)
======================================================================

Primary task (VS Code):
NCAAM: Pregame totals (cached model) [READ-ONLY]

Game spec format:
home,away,line,label,tipoff

Examples:
michigan-st,uconn,136.5,Michigan State vs UConn,7:45 PM CST
tennessee,iowa-st,139.5,Tennessee vs Iowa State,9:25 PM CST

Direct runner command:
powershell -NoProfile -ExecutionPolicy Bypass -File Scripts/vscode_task_runner.ps1 -Action pregame_cached -Window 5 -GameSpec "michigan-st,uconn,136.5,Michigan State vs UConn,7:45 PM CST" -GameSpec2 "tennessee,iowa-st,139.5,Tennessee vs Iowa State,9:25 PM CST"


END OF MASTER HANDOFF
